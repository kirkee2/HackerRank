#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int total,query;
    cin>>total>>query;
    string a[total];
    string b[query];

    string tmp;
    getline(cin,tmp);

    for(int i = 0 ; i<total ; i++){
        getline(cin, a[i]);
    }

    for(int i = 0 ; i<query ; i++){
        getline(cin, b[i]);
    }


    for(int i = 0 ; i<total ; i++){
        if(a)
    }

    /*
    for(int i = 0 ; i<total ; i++){
        cout<<a[i]<<endl;
    }

    cout<<endl;

    for(int i = 0 ; i<query ; i++){
        cout<<b[i]<<endl;
    }
    */

    return 0;
}
